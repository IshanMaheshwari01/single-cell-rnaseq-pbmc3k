# ---------------------------------------------------------------------------
# Python dependencies for the PBMC 3k single-cell RNA-seq project.
#
# Install with:
#     pip install -r requirements.txt
#
# scanpy[leiden] pulls in the Leiden community-detection backend
# (leidenalg + python-igraph) alongside the core single-cell toolkit.
# Version floors below are conservative; the analysis was developed and
# tested with the exact versions noted in the trailing comments.
# ---------------------------------------------------------------------------

scanpy[leiden]>=1.10      # core single-cell toolkit            (tested 1.12.1)
anndata>=0.10             # annotated data matrix container      (tested 0.12.16)
umap-learn>=0.5           # UMAP embedding                       (tested 0.5.12)
scikit-learn>=1.3         # PCA, neighbours, utilities           (tested 1.8.0)
numpy>=1.24               # arrays / linear algebra              (tested 2.4.4)
scipy>=1.10               # sparse matrices, statistics          (tested 1.17.1)
pandas>=2.0               # marker tables, cell-type counts      (tested 2.3.3)
matplotlib>=3.7           # all figures                          (tested 3.10.8)
leidenalg>=0.9            # Leiden clustering algorithm          (tested 0.12.0)
igraph>=0.10              # graph backend for Leiden             (tested 1.0.0)
numba>=0.57               # JIT acceleration used by UMAP/scanpy (tested 0.65.1)

# Notebook tooling (needed only for `make notebook` / running the .ipynb)
jupyterlab>=4.0
nbconvert>=7.0
nbformat>=5.9
ipykernel>=6.0
